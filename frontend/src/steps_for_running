# this is the step for the bash for running the frontend setup
# Create React app
npx create-react-app frontend
cd frontend

# Install additional dependencies
npm install lucide-react

# Replace src/App.js with the React component from artifact
# Update API_BASE URL in the component to point to your FastAPI backend

# Start development server
npm start
